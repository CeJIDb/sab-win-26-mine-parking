# Ретро: переработка структуры Демо 5

**Дата:** 2026-05-01
**Длительность:** ~1 час (4 агента параллельно + фаза 6 последовательно)
**Связанный план:** [plans/2026-05-01-demo-5-rework.md](../../../plans/2026-05-01-demo-5-rework.md)
**Агент:** Claude Code — смешанная сессия (4 параллельных подагента + основной агент для фазы 6)

## 1. Задача

Перераспределить блоки и артефакты Демо 5 между спикерами под логику «проблема → решение»: добавить блок 1.5 (Opportunity Canvas), перенести блок 4 к mrneatly, урезать блоки 3 и 5c, переименовать ADR-слайд.

## 2. Как решал

Сессия разбита на 4 параллельных агента по файловым зонам (A: slides-map.md, B: 2026-04-25-demo-5-final.md, C: talk-structure.md + demo-5-script.md, D: связанные документы). После их завершения — последовательная фаза 6: линтеры, сквозная сверка, точечные доправки.

Параллелизация по файлам исключила конфликты и позволила завершить фазы 1–5 за один прогон. Фаза 6 дала 5 доправок, которые агенты не поймали: тайминги subgraph-заголовков в Mermaid (блоки 0, 5а, 6), строка блока 3 в таблице тайминга в talk-structure.md и секция «Что резать» для блока 3.

## 3. Решил ли

Да, полностью. Все пункты DoD из плана закрыты.

## 4. Эффективность

- Параллельная схема «1 агент = 1 файл» работает надежно: конфликтов не было, все агенты завершили без ошибок линтинга.
- Агенты аккуратно поймали несогласованности в исходниках (pie chart, переход Denis → cejidb, счетчик «9 блоков»), но пропустили два класса проблем: (1) тайминги в заголовках subgraph Mermaid не сверялись с talk-structure; (2) таблица тайминга в talk-structure для блока 3 не была полностью очищена от удаленных артефактов. Стоит явно указывать агентам «сверь subgraph-заголовки с таблицей тайминга» и «пройдись по каждой ячейке таблицы».
- Сломанная ссылка `#фразы-мостики-между-спикерами` → `#фразы-мостики-между-блоками` (агент C переименовал раздел, агент B не обновил якорь) поймана `lint:md-links` — линтер отработал как ожидалось.

## 5. Как было и как стало

**До:** 7 файлов с раскладкой «cejidb на блоке 4, Denis на 5b, блок 1.5 отсутствует, ADR-слайд называется "без брокеров"».

**После:** блок 1.5 добавлен (skifup, Impact Map → OC как замок), mrneatly ведет блоки 4+5c единым нарративом, Denis урезан до 4 мин с 4 артефактами, ADR-слайд переименован в «Транспорт асинхронных уведомлений: Outbox-паттерн vs Kafka vs RabbitMQ — обоснование выбора». Все 4 линтера зеленые, ретро записано.
